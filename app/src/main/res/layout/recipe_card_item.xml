<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/cardview_height"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/tools"
    android:adjustViewBounds="true"
    app:ignore="NamespaceTypo"
    android:layout_margin="@dimen/layout_margin"
    android:orientation="horizontal"
    card_view:cardCornerRadius="@dimen/card_recipe_radius"
    card_view:cardElevation="@dimen/cardview_elevation"
    >

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        >

        <ImageView
            android:id="@+id/recipe_item_iv"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitXY" />

        <TextView
            android:id="@+id/recipe_item_tv"
            style="@style/list_item_text_style"
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:layout_alignBottom="@id/recipe_item_iv"
            android:layout_alignParentStart="true"
            android:layout_marginStart="0dp"
            android:layout_marginBottom="0dp"
            android:background="@color/main_recipe_item_bg"
            android:gravity="center_horizontal"
            android:paddingTop="@dimen/padding_recipe_item"
            android:paddingBottom="@dimen/padding_recipe_item"
            android:textColor="@color/colorPrimaryDark"
            android:textSize="@dimen/text_size_recipe_item"
            android:textStyle="bold"
            tools:text="Burger" />
    </RelativeLayout>


</android.support.v7.widget.CardView>
